# DeepSeek ReAct 智能体 - Python 依赖包

# HTTP 请求库 - 用于调用 DeepSeek API
requests>=2.31.0,<3.0.0

# 环境变量管理 - 用于加载 .env 文件中的配置
python-dotenv>=1.0.0,<2.0.0

# 彩色终端输出（可选但推荐）- 用于美化 CLI 界面
colorama>=0.4.6,<1.0.0

# OpenAI 官方 SDK - 用于调用 OpenAI API
openai>=1.0.0

# Anthropic 官方 SDK - 用于调用 Claude API
anthropic>=0.18.0

# Google Generative AI SDK - 用于调用 Gemini API
google-genai

# FAISS 向量数据库 - 用于长期记忆语义搜索
faiss-cpu>=1.7.0,<2.0.0

# 系统监控 - 用于健康检查
psutil>=5.9.0,<6.0.0

# Model Context Protocol Python SDK - required by dm_agent/rag/rag_mcp_server.py
mcp>=1.0.0,<2.0.0
